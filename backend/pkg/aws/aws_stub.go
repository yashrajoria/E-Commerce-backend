package aws

// aws_stub.go
// Minimal placeholder to satisfy imports during CI when local replace points to ../../pkg/aws.

// Add real implementations here when migrating shared AWS helpers into a proper package.
