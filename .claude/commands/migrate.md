---
description: Run pending SQL database migrations against PostgreSQL using golang-migrate
---

Execute SQL database migrations:

1. Change directory to `backend/`.
2. Run `./scripts/migrate.sh up`.
3. Check PostgreSQL connection status and report applied migration scripts.
